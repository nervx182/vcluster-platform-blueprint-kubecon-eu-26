kube-prometheus-stack:
  global:
    imageRegistry: ""
  prometheusOperator:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
    prometheus:
      monitor:
        additionalLabels:
          monitoring.instance: vcluster-workshop-test
  prometheus:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
      paths:
        - /prometheus
      pathType: ImplementationSpecific
      hosts:
        - vcluster-workshop-test.83.167.196.210.traefik.me
      tls:
        - secretName: tls-prometheus
          hosts:
            - vcluster-workshop-test.83.167.196.210.traefik.me


    prometheusSpec:
      retention: 10d
      serviceMonitorSelector:
        matchLabels:
          monitoring.instance: vcluster-workshop-test
      storageSpec:
        volumeClaimTemplate:
          spec:
            accessModes:
              - ReadWriteOnce
            resources:
              requests:
                storage: 10Gi
      externalUrl: https://vcluster-workshop-test.83.167.196.210.traefik.me/prometheus
      routePrefix: /prometheus
    thanosServiceMonitor:
      serviceMonitor:
        additionalLabels:
          monitoring.instance: vcluster-workshop-test
  alertmanager:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
      paths:
        - /alertmanager
      pathType: ImplementationSpecific
      hosts:
        - vcluster-workshop-test.83.167.196.210.traefik.me
      tls:
        - secretName: tls-alertmanager
          hosts:
            - vcluster-workshop-test.83.167.196.210.traefik.me


# Below is an example alertmanager.config section:
# It demonstrates an advanced routing setup that includes Microsoft Teams (v2), a watchdog receiver
# (e.g. for UptimeRobot) and a silenced route for informational alerts.
# This config use webhook urls from secret-files (see alertmanagerSpec below)
#
#    config:
#      route:
#        receiver: msteams-notifications  # Default receiver if no route matches
#        routes:
#          # 1. Silence all alerts named "InfoInhibitor"
#          - receiver: "null"
#            matchers:
#              - alertname =~ "InfoInhibitor"
#          # 2. Route "Watchdog" alerts to UptimeRobot with a short repeat interval
#          - receiver: uptimerobot
#            repeat_interval: 1m
#            matchers:
#              - alertname =~ "Watchdog"
#          # 3. Route all alerts with severity "warning" or "critical" to Microsoft Teams
#          - receiver: 'msteams-notifications'
#            matchers:
#              - severity =~ "warning|critical"
#            continue: true  # Continue to evaluate other routes after this one
#      receivers:
#        # Microsoft Teams v2 receiver using `msteamsv2_configs`
#        - name: 'msteams-notifications'
#          msteamsv2_configs:
#            - send_resolved: true
#              webhook_url_file: /etc/alertmanager/secrets/msteams-webhook-url
#        # Watchdog/Uptime monitoring receiver
#        - name: uptimerobot
#          webhook_configs:
#            - url_file: /etc/alertmanager/secrets/uptimerobot-webhook-url
#        # Null receiver to discard matched alerts
#        - name: "null"
#      # Optional: Use custom templates for consistent alert formatting
#      templates:
#        - /etc/alertmanager/config/*.tmpl

    alertmanagerSpec:
#      # secrets for alertmanager config webhook urls
#      secrets:
#        - msteams-webhook-url
#        - uptimerobot-webhook-url
      externalUrl: https://vcluster-workshop-test.83.167.196.210.traefik.me/alertmanager
      routePrefix: /alertmanager

  kube-state-metrics:
    prometheus:
      monitor:
        additionalLabels:
          monitoring.instance: vcluster-workshop-test
  kubeApiServer:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
  kubelet:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
  kubeControllerManager:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
  coreDns:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
  kubeDns:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
  kubeScheduler:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
  thanosRuler:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: vcluster-workshop-test
  grafana:
    admin:
      existingSecret: grafana-admin-credentials
      userKey: admin-user
      passwordKey: admin-password
    serviceMonitor:
      labels:
        monitoring.instance: vcluster-workshop-test
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
      path: /grafana
      pathType: ImplementationSpecific
      hosts:
        - vcluster-workshop-test.83.167.196.210.traefik.me
      tls:
        - secretName: tls-grafana
          hosts:
            - vcluster-workshop-test.83.167.196.210.traefik.me
    envFromSecrets:
      - name: oauth2-credentials
    grafana.ini:
      server:
        root_url: https://vcluster-workshop-test.83.167.196.210.traefik.me/grafana/
        serve_from_sub_path: true
    additionalDataSources:
      - access: proxy
        isDefault: false
        name: loki
        type: loki
        url: http://loki-headless.loki.svc.cluster.local:3100
        version: 1
prometheus-blackbox-exporter:
  global:
    imageRegistry: quay.io

externalSecrets:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vcluster-workshop-test

  secretStores:
    vcluster-workshop-test:
      kind: ClusterSecretStore
      name: vcluster-workshop-test

  secrets:
    grafana-admin-credentials:
      secretStoreRef: vcluster-workshop-test
      data:
        - remoteKey: vcluster-workshop-test-grafana-admin-user
          remoteKeyProperty: admin-user
          secretKey: admin-user
        - remoteKey: vcluster-workshop-test-grafana-admin-password
          remoteKeyProperty: admin-password
          secretKey: admin-password

namespace:
  labels:
    project-name: "vcluster-workshop"
    stage: "test"
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
